function FindProxyForURL(url, host) {

    if (isInNet(host, "192.168.0.0", "255.255.0.0")) {
        return "DIRECT";
    }

    if (dnsDomainIs(host, "youtube.com") || shExpMatch(host, "*.youtube.com")) {
        return "DIRECT";
    }

    return "PROXY 192.168.1.167:8080";
}
